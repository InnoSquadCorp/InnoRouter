# InnoRouter 6.1.1 release checklist

This checklist accompanies the 6.1.1 patch release cut. The exact published
commit, workflow outcome, and site state must be taken from the tag and release
run; this source document does not claim that publication has already happened.

## Scope and compatibility

- No public symbol is added, removed, or changed. The three public API
  baselines and symbol budgets are unchanged.
- `@TabItem` default scope IDs equal the generated `Tab` raw value in both
  expansion paths. Only a router that combines a backticked tab case with a
  sibling `id:` expands differently; its 6.1.0 snapshots keep that branch as an
  orphan unless the tab declares the old spelling with `id:`.
- `RouterTabHost(store:)` pushes default links into the tab it displays, and
  selects that tab in the same transition when the stored selection is orphaned.
- The supplied-store tab and split host initializers render a mismatched root
  instead of trapping, and log one warning per host type. Over a root of
  another shape their content binds to an unresolvable scope, and their
  default links and a tab host's tab bar selection are rejected, so nothing in
  the host writes into that root.
  Exact restoration still applies any root shape, as pinned by the restoration
  boundary tests.
- `RouterRestorationDriver` applies `RouterSnapshotRecoveryPolicy` to a typed
  storage rejection and restores the fallback through normal policies. `.fail`
  and untyped storage errors keep their 6.1.0 behavior; the partial-validation
  driver still applies no recovery.
- `swift-syntax` admits `"603.0.2"..<"605.0.0"`. The committed resolution stays
  on 603.0.2; the platform workspace resolves the newest admitted line.
- Swift 6.3 and the existing Apple platform minimums are unchanged.
- 6.1.0's three `RouterSnapshotError` cases remain, recorded as a known source
  issue, and `RELEASING.md` now classifies enum case additions as breaking.

## Regression evidence

Each fix has a focused test that failed on the 6.1.0 implementation for the
intended invariant and passes on this candidate:

| Defect | Test | Failure on 6.1.0 source |
| --- | --- | --- |
| Backticked tab ID beside `id:` | `escapedTabIDBesideExplicitID`, `escapedCaseDuplicateEffectiveTabID`, `escapedTabIdentityIgnoresSiblingExplicitID` | Expansion emitted ``RouterScopeID("`default`")``; E009 missed the collision |
| Link under an orphaned selection | `orphanedSelectionLinkTargetsDisplayedTab` | Plan kept selection `legacy` and pushed into the hidden branch |
| Host root-shape traps | `nonTabRootDoesNotAbort`, `nonSplitRootDoesNotAbort`, `mismatchedColumnCountDoesNotAbort` | `preconditionFailure` at `RouterTabHost.swift:135` and `RouterSplitHost.swift:135,193` |
| Host writes into a root of another shape | `mismatchedRootRejectsHostWrites` (split and custom roots), `mismatchedRootRejectsSplitLinks`, `mismatchedRootRejectsColumnNavigation` | Without the guards and the unresolvable content scope, links, tab bar selection, and tab or column content navigation wrote into same-named branches |
| Storage rejection skipped recovery | `storageRejectionUsesRecoveryPolicy` | Activation threw `encodedDataTooLarge` despite `.use` |
| swift-syntax 604 library evolution | Release `BUILD_LIBRARY_FOR_DISTRIBUTION=YES` build of `InnoRouterMacroFirstSmoke` | The CI error at `RouterMacro.swift:377` |

`untypedStorageFailureSkipsRecoveryPolicy` is a control that passes before and
after: an untyped storage failure still fails activation.

## Release gates

- [x] Runtime identity and both README installation versions say 6.1.1.
- [x] CHANGELOG has a dated 6.1.1 section and a fresh Unreleased section.
- [x] Both macro suites pass on swift-syntax 603.0.2 and 604.0.0, and the
  library-evolution consumer build passes on 604.0.0.
- [x] The five Inspector UI tests pass in two consecutive local runs of the
  final sources on an iPad mini (A17 Pro) iOS 26.5 simulator, with the updated
  result checker. An earlier draft of the wait helper spent its timeout on a
  separate existence wait, and XCTest's roughly one-second predicate polling
  then left one hittable wait without an evaluation; the final helper checks
  once before polling and waits for the full timeout.
- [x] Local principle gates on the release candidate: 704 package tests in 88
  suites (the existing intentional known issue remains), public API baselines
  and budgets unchanged at 1193/207/252, DocC, documentation, example,
  consumer, probe, and lint gates. After the review fixes, 710 package tests,
  the public API baselines, and the documentation and lint gates pass again.
- [ ] All required checks on the final PR head and merged main commit,
  including the new forward `xcode-27` job.
- [ ] Bare `6.1.1` tag points to the verified main commit.
- [ ] Release workflow and exact tagged consumer pass before publication.
- [ ] GitHub Release, versioned DocC, and `/latest/` resolve to 6.1.1.

The last four items are recorded by GitHub's immutable commit/run evidence and
the publication completion report, after this document is committed.

Local validation used Xcode 27.0 / Swift 6.4. Remote checks independently use
the pinned Xcode 26.6 / Swift 6.3; the forward `xcode-27` job covers Xcode 27
remotely.

## Follow-up quality work

The 6.1.0 release run failed its first attempt on two jobs. The visionOS job
timed out in `simctl bootstatus`; boots now shut down, erase, and retry. The
Inspector UI job timed out waiting for the fixture's locale label after a
synthesized tap on `probe.locale.ko` at the button's center; the label stayed
`Locale en` for five seconds after the app went idle, which points to a lost
fixture tap rather than slow rendering. The same test had failed three earlier
first attempts on the nested switch the 6.1.0 fixture change removed. Splitting
the scenarios and printing recorded assertions make the next failure cheaper
and diagnosable; they do not prove the lost-tap cause is gone. No retry or
longer timeout was added.

Topology reconciliation failures (`rootIsNotTabs`, `expectedStack`) still
bypass `RouterSnapshotRecoveryPolicy.use`. Expressing them to the recovery
closure needs a new `RouterSnapshotError` case, which the corrected policy
defers to the next major release.

Dependabot PR #50 moves the swift-syntax floor to 604.0.0 and is superseded by
the widened range.
