# InnoRouter 6.0.0 Release Checklist

> 2026-09-11: [열두 번째 재검토](6.0.0-twelfth-review-remediation-plan.ko.md)에서
> 추가 runtime/macro 결함 6건이 재현되어 로컬 구현 완료 판정을 철회했다.
> 해당 계획의 T00~T07과 회귀 C01~C25가 통과하기 전에는 이 체크리스트를
> 배포 후보 승인으로 해석하지 않는다.

- Candidate status: eleventh-review remediation implemented and validated locally; external publication gates remain
- Verified: 2026-09-11
- Active local toolchain: Xcode 27.0 / Swift 6.4
- Release toolchain: Xcode 26.6 / Swift 6.3

Current follow-up (2026-09-11): the
[eleventh-review remediation plan](6.0.0-eleventh-review-remediation-plan.ko.md)
closed six additional Feature ownership, pending-link lineage, immersive restore,
macro availability/conditional-declaration, and CI dependency findings locally.
Its [regression traceability](review-evidence/6.0.0-eleventh-review.ko.md) records
the completed local implementation, corrected tenth-review claims, and post-fix
verification. Exact Xcode 26.6 / Swift 6.3, native interaction, exact remote
candidate, approval, and publication remain separate gates.

The completed evidence below applies to the current implemented candidate,
including the [next capabilities](6.0.0-next-capabilities-plan.ko.md) and the
[second review remediation](6.0.0-second-review-remediation-plan.ko.md).
Those design documents remain Draft until maintainer approval; implementation
status is recorded separately from approval status.

The [third review remediation plan](6.0.0-third-review-remediation-plan.ko.md)
records seven reproduced defects and their completed local fixes. The linked
[evidence](review-evidence/6.0.0-third-review.ko.md) separates current Swift 6.4
results from the pinned release toolchain, native UI, remote, adopter, approval,
and publication gates that remain open.

## Completed locally

- [x] Full Swift package, macro expansion, runtime, regression, testing, and
  inspector suites pass.
- [x] The selectable public library products are exactly `InnoRouter`,
  `InnoRouterTesting`, and `InnoRouterInspector`.
- [x] Public API baselines match and retired 5.x authority symbols are absent
  from the external surface.
- [x] README and DocC Swift examples typecheck; all three public DocC catalogs
  build without warnings.
- [x] Independent downstream packages compile the macro-first runtime and test
  the Testing plus Inspector product combination.
- [x] Macro-first and developer-tools consumers compile for iOS/iPadOS, Mac
  Catalyst, macOS, tvOS, watchOS, and visionOS through the explicit platform
  workspace; all three public products emit validated library-evolution
  interfaces at their declared floors.
- [x] The shared capability/adaptation runtime suite is wired for iPhone, iPad,
  tvOS, watchOS, and visionOS simulators; macOS executes it in the package suite.
- [x] SwiftFormat, strict SwiftLint, source responsibility budgets, privacy
  checks, and fail-fast environment behavior pass.
- [x] Deterministic state-machine/input-mutation suites and explicit event,
  store, scope, and Inspector ownership tests are part of the package suite.
- [x] The gated runtime coverage report exceeds the 85% floor and the
  comprehensive report keeps macro-plugin coverage visible.
- [x] Focused Thread Sanitizer and Address Sanitizer suites pass locally.
- [x] Seven release-mode reducer, snapshot, deep-link, Inspector,
  scenario-capture, history-capacity, and catalog-size performance baselines
  produce a validated JSON trend artifact.
- [x] Scenario fixture v6 rejects incompatible route/environment/dependency
  metadata and malformed request/deferral graphs before requests, preserves
  history navigation semantics and relative revision constraints across queues
  and repeated rebases, records explicit request-versus-deferral cancellation,
  maps fresh deferral IDs, and generates separate source and JSON artifacts
  that execute in a new process. Format v5 and unknown versions fail with an
  explicit version error. Feature-plan fixtures additionally require exact,
  macro-mapping-derived ownership resolvers and reject missing, duplicate, or
  tampered resolver paths before execution.
- [x] Parent macro catalogs compose feature catalogs and matchers, and the
  Inspector exposes payload-safe sharing plus opt-in scenario recording UI.
- [x] Independent downstream fixtures build the exact published 5.2.1 API and
  its current macro-first 6.0 migration, then compare equivalent behavior.
- [x] The [quality follow-up](6.0.0-quality-review.md) closes snapshot integer
  and version boundaries, preserves per-action replay context, reopens bounded
  diagnostic bundles, and verifies signpost interval ownership and teardown.
- [x] Shell syntax, workflow YAML, and whitespace validation pass.
- [x] R6R3-F01 through R6R3-F07 have focused counterexample regressions and pass
  the full local suite, downstream consumer, generated-scenario, sanitizer,
  coverage, performance, platform build, and interface gates.
- [x] R6R10-F02 through R6R10-F08 have permanent counterexample regressions and
  pass the full local package, macro-first consumer, generated replay, API,
  documentation, sanitizer, coverage, performance, platform, and interface gates.
- [x] R6R10-F01 has a compiler-safe isolated-deinit refactor and passes the local
  Swift 6.4 Release, lifecycle, performance, platform, and consumer controls.
- [x] R6R11-F01 through R6R11-F06 have permanent counterexample regressions and
  pass the local package, macro-first consumer, generated replay, API, DocC,
  sanitizer, coverage, performance, platform, and interface gates.
- [x] The final gated coverage is 88.24% (14,127/16,010 lines); comprehensive
  coverage is 81.24% (16,277/20,035 lines).

Canonical local command:

```sh
./scripts/principle-gates.sh --platforms=all
```

## Required before publication

- [x] Resolve R6R3-F01 through R6R3-F07 and verify the acceptance criteria in the
  [third review remediation plan](6.0.0-third-review-remediation-plan.ko.md).
- [x] Implement R6R10-F01 through R6R10-F08 and complete the locally executable
  acceptance evidence in the [tenth review plan](6.0.0-tenth-review-remediation-plan.ko.md).
- [x] Implement R6R11-F01 through R6R11-F06 and complete the locally executable
  acceptance evidence in the [eleventh review plan](6.0.0-eleventh-review-remediation-plan.ko.md).
- [ ] Re-run R6R10-C01 with the exact pinned Xcode 26.6 / Swift 6.3 toolchain and
  prove that the original optimized `RouterHistory` compiler crash is resolved.
- [ ] Execute R6R10-C19 against real macOS/iPadOS window and visionOS immersive
  lifecycles, including allow, reject, and cancellation restoration paths.
- [ ] Review and approve the Draft capability/remediation documents and their
  linked local acceptance evidence.
- [ ] Run all required workflows using the pinned Xcode 26.6 / Swift 6.3
  release toolchain, including independent sanitizers and the runtime
  performance artifact.
- [ ] Complete maintainer review of the full breaking API and repository diff.
- [ ] Replace `Unreleased` only in the release commit and use the intended
  release date.
- [ ] Create and push the exact `6.0.0` tag only after the release commit is on
  the intended branch.
- [ ] Run the independent consumer against the exact published revision.
  Not run until a 6.0.0 tag exists.
- [ ] Verify release notes, generated documentation, package resolution, and
  remote branch/tag parity after publication.

This checklist records evidence; it does not claim that 6.0.0 is published.
